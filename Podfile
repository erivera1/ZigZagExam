# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ZigZagExam' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ZigZagExam
  # Pods for API Bindings
  pod 'SwiftyJSON'
  pod 'Alamofire'
  pod 'AlamofireObjectMapper'
  pod 'Kingfisher', '~> 5.0'
  pod 'PKHUD', '~> 5.0'

  target 'ZigZagExamTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ZigZagExamUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
